# 第一轮实机采集后的资源入口追踪

40 条 CRip008 绑定的精确长度离线回放均通过；未观察到的是实际调用方的
长度／补齐参数。这不是 40 个已确认故障。另有 PM 4 项只出现加载记录，
不能计为绘制通过，也不能由此判为漏替换。

本轮重新读取原始归档中的 13 个相关 CRsa 记录，核对记录哈希、解密校验、
明文哈希，重新原生解析命令；逐个核对图片的八字节身份及所在的类型字段。
采用既有 466 个 CRsa 记录的引用普查作定位索引，没有把它扩称为所有脚本格式。
两张 Logo 所在官方映射表也重新核对了归档字节。

[逐项证据](../../evidence/photon/images/static-review-20260909/pending-script-routes.json)
保留全部 44 项、13 个脚本身份、资源参数位置、脚本之间的调用及中文定位句。
所有条目的实机状态仍为 `still_unverified`。

## 40 项的入口范围

| 资源 | 已证明的脚本范围 | 后续采集方式 |
| --- | --- | --- |
| PF 18 项军事 HUD／兵装标识 | 《继承》《告白》及其两组兵装辅助脚本合起来引用全部 18 项；《雨舞者》《小鸡潜降兵》也有部分调用 | 从对应剧情存档进入驾驶舱／战斗演出，采集实际绘制日志 |
| PM 18 项军事 HUD／兵装标识 | 《憧憬》的剧情及两组兵装辅助脚本引用全部 18 项 | 从对应剧情存档进入演出，采集实际绘制日志 |
| PF G2787／G2803 | 《樱花盛开之前》同一脚本，命令 274／1948 调用背景辅助脚本 | 进入毕业典礼及后续校门场景，不能用相册 G1972 代替 |
| PF G1668／PM G3003 篇章 Logo | 官方映射表中有资源关系；已普查 CRsa 内没有直接引用 | 显示入口仍待定位，不安排猜测性的实机路线，也不判为废弃资源 |

前四个剧情片段（PF《继承》《告白》《樱花盛开之前》、PM《憧憬》）及辅助
脚本合计覆盖 38 项的**静态引用／登记**。这不是“一次播放四段就必然绘制
38 项”的保证：语言、状态、选用兵装及原生控件仍可能决定实际使用的成员。

相册蓝／红线框图 G1661／G1662 是静态装备说明图，不是这些 HUD。
相册已观察到的毕业礼堂 G1972 是 1280×996；待确认的 G2787 是 900×700，
G2803 是 933×718。外观相近不能代替身份命中。

## 存档定位用的对白

这些是图片引用命令之后出现的中文定位句，不是游戏提供的跳转点。
工具从原生命令读取文本池，用日文原文 UTF-8 哈希匹配现有中文审核稿；
没有用可能随版本变化的 CSV 偏移去猜对白位置。相同原文对应多个不同译文
时不选用该句。章节 CSV 名称采用项目现有分类，不声称是游戏内子章节名称。

| 剧情片段 | 图片引用后可辨认的句子 |
| --- | --- |
| PF《继承》 | Lightning 2：“终于要开始了……都快等得不耐烦了！” |
| PF《告白》 | 竹宫：“是音振欺骗筒！传感器会失效的！” |
| PF《樱花盛开之前》礼堂 | “寒意料峭的冬天也即将结束……” |
| PF《樱花盛开之前》校门 | “小武，我们要永远永远在一起哦。” |
| PM《憧憬》后段 | “那时的我还是个孩子。” |

后续应先匹配用户已有的剧情存档，再决定从哪里开始录日志；这些句子只是
缩小存档寻找范围。若必须长时间重播，则继续找其他已证明的入口，不把
“静态覆盖四段”误写成“实机只需点击四次”。

## PM 的四项加载未绘制

| 项目 | 原生命令证据 | 当前结论 |
| --- | --- | --- |
| G2946／G2961 存取档手柄提示 | 预加载脚本命令 71／89 均为 `OM_SpecifyPrefetchCommand` | 加载符合预取行为；原生控件的绘制／输入模式入口仍未证明 |
| G2971 语音回放手柄提示 | 同一预加载脚本命令 99 | 同上，不继续要求通过普通相册入口触发 |
| G492 壁纸解锁提示 | 预加载命令之外，独立脚本命令 17 将图片传入辅助脚本；此前读取 `PM_WPOPEN_` 对应状态并判断，随后更新状态 | 应针对尚未解锁的壁纸事件采集；普通打开相册不能保证显示 |

未改动解锁状态或用户存档，不要求重置进度。现有存档若已经解锁，需另找
合适入口后再安排；重复浏览已解锁壁纸不能充当该提示已验证的证据。

## 后续判定

实际捕获后继续按精确资源身份对账：成功提交且回读一致才记通过；
已加载未绘制、未触发、明确拒绝分别记录。只有出现真实长度或矩形不匹配
证据时才修改对应 hook，不为 40 项预先放宽身份判断。

本轮没有启动游戏、没有使用 computer use、没有写入游戏安装目录。
正常 DLL 的恢复状态沿用第一轮采集记录，未重新安装诊断版。

复现命令（私有普查目录及原始归档需在本地可用）：

```powershell
python -m rUGP.tools.images.trace_pending_script_references --census-root <private-census-directory> --output <report.json>
```

工具及报告只针对本轮已登记的 40＋4 项；不是通用的完整游戏可达性分析器。
公开仓库仅保存代码及元数据，原始归档、脚本明文、图片与日志仍留本地。
